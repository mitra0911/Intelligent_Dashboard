/**
 * White-label app shell — change these values to rebrand the UI.
 * (HTML `<title>` in `index.html` should stay in sync for static loads.)
 */
export const BRAND_NAME = 'Analytics Dashboard'

/** Second line under the title in the sidebar mark. */
export const BRAND_TAGLINE = 'Live metrics & topology'

/** Sidebar subtitle (integration / demo layer). */
export const BRAND_SUITE_PRIMARY = 'ObservIQ'
export const BRAND_SUITE_SECONDARY = 'overlay'
